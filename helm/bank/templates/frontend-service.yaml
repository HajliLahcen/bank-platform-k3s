apiVersion: v1
kind: Service

metadata:
  name: bank-frontend
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: bank-frontend

  ports:
    - protocol: TCP
      port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.port }}

  type: ClusterIP
